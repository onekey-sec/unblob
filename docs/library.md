# Using Unblob as a library

Unblob is not just a command line tool, it can be used as a Python library too.  
It has a very simple API interface, which you can call from Python programs.

## API reference
